name: delta

services:
  mariadb:
    image: 'mariadb:10.11'
    container_name: delta-mariadb
    restart: unless-stopped
    ports:
      - '${DB_PORT:-3306}:3306'
    environment:
      MYSQL_ROOT_PASSWORD: '${DB_PASSWORD:-admin}'
      MYSQL_DATABASE: '${DB_DATABASE:-database}'
      MYSQL_ALLOW_EMPTY_PASSWORD: 'no'
    volumes:
      - delta-mariadb-data:/var/lib/mysql
    networks:
      - delta-network
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      retries: 3
      timeout: 5s

  phpmyadmin:
    image: 'phpmyadmin:latest'
    container_name: delta-phpmyadmin
    restart: unless-stopped
    ports:
      - '8080:80'
    environment:
      PMA_HOST: mariadb
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: '${DB_PASSWORD:-admin}'
    networks:
      - delta-network
    depends_on:
      mariadb:
        condition: service_healthy

networks:
  delta-network:
    driver: bridge

volumes:
  delta-mariadb-data:
    driver: local
